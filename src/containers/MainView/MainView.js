import MediaTracks from '@/containers/MediaTracks';
import RoomControls from '@/containers/RoomControls';

export default {
  name: 'MainView',
  components: {
    MediaTracks,
    RoomControls,
  },
};
